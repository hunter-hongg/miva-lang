[package]
name = "miva-frontend"
version = "0.1.0"
edition = "2021"
description = "Miva language frontend (lexer + parser) — Rust rewrite of the OCaml frontend"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
