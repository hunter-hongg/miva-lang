module "std.str"

concat = (ref a: string, ref b: string): string => string_concat(a, b)

parse_int = (ref str: string): int => string_parse(str)

trusted len = (ref str: string): int => string_length(str)

export concat 
export parse_int
export len